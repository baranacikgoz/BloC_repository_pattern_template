import 'package:flutter/material.dart';

/// Class to hold static icons.
class AppIcons {
  /// Wifi on Icon
  static const wifiOn = Icon(Icons.wifi);

  /// Wifi off Icon
  static const wifiOff = Icon(Icons.wifi_off);

  /// Cellular on Icon
  static const cellularOn = Icon(Icons.signal_cellular_alt);

  /// Cellular off Icon
  static const cellularOff = Icon(Icons.signal_cellular_off);

  /// Incremet icon
  static const increment = Icon(Icons.add);

  /// Decrement icon
  static const decrement = Icon(Icons.remove);

  /// Darkmode icon
  static const darkMode = Icon(Icons.dark_mode_outlined);

  /// Lightmode icon
  static const lightMode = Icon(Icons.light_mode);
}
